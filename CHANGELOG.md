# 更新日志

## v0.2.138 — 节日只认当天，问候只说一次

- 法定假期的假期区间不再整段当成节日当天：只有节日当天报节日名（用节日自己的图标），假期的其余天数报「X假期」，假期第二天起不会再把节又过一遍、也不再重复触发节日问候与节日氛围。
- 按国务院 2026 年放假安排订正内置假期数据：元旦、春节、端午补齐假期天数，国庆去掉多算的 10 月 8 日，补上 1 月 4 日、9 月 20 日两个调休上班日；清明节气日期改为 4 月 5 日。DeepSeek 峰谷判定仍按整段假期算全天低谷，订正后补进来的假期日也归低谷。
- 节日问候改为「说出口才算完成」：只有助手的可见回复里真的提到节日（节日名或具体意象）才收起提醒；工具调用、被中断的回答不算完成；还没提到时，会跨过注入间隔继续轻轻提醒，不抢正事。
- 情境注入新增「单独关闭」：可以只让某几位伙伴不带拾光记情境，其余伙伴与全局开关不受影响。
- 节日文案里的称呼改从用户配置读取，不再写死某一个名字。
- 自动测试：421/421 通过；66 个 JavaScript 源文件 `node --check` 通过。

## v0.2.133 — 节日问候更自然，也更守时

- 节日问候引导覆盖全部用户回合，避免注入间隔导致后续合适时机失去提醒；闲聊时自然表达祝福并分享一处具体意象，正事优先、不强制。
- 节日氛围遵守当前时刻：白天不把夜色或赏月写成已经发生，晚间也不在缺少依据时断言月亮可见。
- 清理与当前节日分寸相冲突的旧引导文案。
- README 的 JavaScript 文件数量说明区分仓库源文件与发布包文件。
- 自动测试：353/353 通过；66 个仓库 JavaScript 文件 `node --check` 通过。

## v0.2.131 — 情境注入加一条事实边界

- 情境注入块末尾统一挂一句事实边界：手上只有今天和这位伙伴的当前材料，明天的天气、还没到的日子、更早的细节、其他窗口里发生的事都不知道，被问到就承认不确定，不顺着猜也不为了圆场补细节。
- 只加一句、只出现一次，不逐个事实块增加提示词预算；覆盖普通、带历史档案、带峰谷三个分支和全部事实块。
- 自动测试：350/350 通过；66 个 JavaScript 文件 `node --check` 通过。

## v0.2.130 — 峰谷规则口径补齐

- DeepSeek 峰谷注入随当前时段一起给出官方区间口径：工作日 09:00-12:00、14:00-18:00 为高峰，其余时间为低谷；周末和法定节假日全天按低谷计费。
- 明确要求不得凭记忆补充旧版错峰时段，避免被追问时段依据时引用早已失效的规则。
- 自动测试：350/350 通过；66 个 JavaScript 文件 `node --check` 通过。

## v0.2.129 — DeepSeek 节假日峰谷规则修正

- DeepSeek 峰谷提醒按官方规则识别法定节假日与周末，全天按谷价处理。
- 调休上班的周末仍保持全天谷价，不再按工作日峰谷时段切换。
- 新增普通工作日、调休周末和法定节假日工作日的边界回归测试。
- 自动测试：350/350 通过；66 个 JavaScript 文件 `node --check` 通过。

## v0.2.128 — 对外今日快照修复

- 修复宿主未传入扩展工厂 `dataDir` 时，对外今日快照不生成的问题。
- 快照写入改为回退到数据实例自身目录，聊天类消费方可正常读取日子账本。

## v0.2.127 — 情绪证据链修复

- 明确的用户情绪自述由本地保留，隐性情绪继续交给模型判断。
- 伙伴遭遇明确夸奖、感谢或责怪时，模型回空可保留一条标记为 `possible` 的候选，并保留原话证据。
- 修复伙伴情绪否定句与混合句误判；成功空结果会清理旧候选，失败不会误删历史。
- 排除 `[hana_context]`、`[hana_reference]` 等内部注入，避免工具说明污染情绪证据。
- 修复做册进度卡在宽窗口下单独拉长的问题。
- 自动测试：350/350 通过；66 个 JavaScript 文件 `node --check` 通过。

## v0.2.117 — 生理期规律名称优化

- 将日历卡片名称从「周期规律」改为「生理期规律」，让功能含义更直观。
- 本次仅调整界面文案，不改变计算逻辑和数据格式。

## v0.2.116 — 周期规律与峰谷提醒收口

- 新增日历「周期规律」：查看历史周期与经期长度、周期统计、下次预计区间、当前周期第几天和超期天数。
- 数据不足三次有效记录时，仅展示历史记录，不生成汇总或预测，避免硬猜。
- 收紧 DeepSeek 峰谷关照：同一聊天框、同一时段只在开场回复中提一次，后续不再反复作为梗或意象延用，真实换班时才再次提醒。
- 自动测试：325/325 通过。
