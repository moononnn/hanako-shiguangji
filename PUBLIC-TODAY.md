# 对外快照（public-today.json）

拾光记对外的只读快照，供别的消费者（聊天类 App 等）读取。**谁能看到哪一段由消费方按契约取**：这份文件只摊平事实，不做裁剪之外的加工。

- 位置：`<HANA_HOME>/plugin-data/shiguangji/public-today.json`
- 刷新：插件启动时写一次；此后数据变动（新增/改日子、确认生理期、生成或修改生活日、天气刷新）后去抖刷新（约 2 秒合并一次）。读到的可能是几秒前的快照，这对消费方足够。
- 当前版本：`schemaVersion: 1`。读之前先检查它，不认识就别解析。

## 形状

```json
{
  "schemaVersion": 1,
  "generatedAt": "2026-09-13T04:51:00.000Z",
  "dataRev": 42,
  "today": {
    "date": "2026-09-13",
    "weekday": "日",
    "festivals": ["中秋节"],
    "events": ["在一起第 500 天"],
    "workday": false,
    "todos": ["给圆宝买狗粮"],
    "period": false
  },
  "weather": { "place": "成都 武侯区", "line": "窗外阴着，风不大，24°C 上下", "temp": 24 },
  "summaries": {
    "hanako": [
      { "date": "2026-09-12", "text": "……" }
    ]
  }
}
```

## 字段

### `today{}`

| 字段 | 说明 |
| --- | --- |
| `date` | 当天日期 `YYYY-MM-DD` |
| `weekday` | 星期几，单字：`日` / `一` … `六` |
| `festivals` | 内置节日名（法定节假日、节气、传统节日等），最多 10 条 |
| `events` | 用户自己记的日子标题（纪念日、自定义日子）。**不含生理期**，生理期单独走 `period` |
| `workday` | 是否调休上班日 |
| `todos` | 今天到期、尚未完成的待办标题，最多 20 条 |
| `period` | 当天是否处于已确认的生理期。用户关掉生理期开关后恒为 `false` |

### `weather{}`

未启用天气、地点不匹配或缓存过期时为 `null`。

| 字段 | 说明 |
| --- | --- |
| `place` | 地点名 |
| `line` | 一句话的「窗外感觉」，给消费方当素材，不要照抄 |
| `temp` | 当前温度（℃，整数）。读不到为 `null` |

### `summaries{}`

按 agent id（伙伴 id）索引。**每位伙伴只有自己那段生活日的回顾**，值是该伙伴最近几个已结束生活日的条目，新的在前。

| 字段 | 说明 |
| --- | --- |
| 键 | 伙伴 agent id，与宿主 `agents/<id>` 一致 |
| `date` | 生活日 `YYYY-MM-DD` |
| `text` | 那一天收好的记录正文 |

- 每人单独计算，底座是最近 3 个已结束生活日，单条目字符预算 1800，最多 6 条。
- 该伙伴没有可用档案时，这个键不出现。
- 花酿来访角色按逻辑角色归档的档案可能对不上宿主 agent id，此时该伙伴取不到回顾，属正常降级。

## 消费方该怎么做

1. 读文件、检查 `schemaVersion`。
2. **只取 `summaries[自己这边的伙伴 id]`**，绝不把别人的档案当自己的记忆用。这是隐私边界，不是可选项。
3. `today` / `weather` 是所有人的共同背景，可直接用。
4. 「今天是特殊日子」这类事实建议按天节制使用（跨天或内容变化时再提），不要每轮都往提示里塞，容易让伙伴反复念叨。
5. 文件不存在、解析失败、字段缺失：**静默降级**，不要报错给用户，聊天照常。

## 边界

- 这份快照**不会发布到互联网**，只存在于本机数据目录里。
- 它**不含**会话原文、不含凭据、不含任何路径穿越风险（所有值都是短文本）。
- 消费方**不应写入**这份文件，也不应依赖其中的字段顺序。
